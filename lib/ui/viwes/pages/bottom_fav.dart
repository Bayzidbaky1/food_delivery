import 'package:flutter/material.dart';

class FavorutePage extends StatelessWidget {
  const FavorutePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:  Container(
        child: Center(child: Text("fav page")),
      ),
    );
  }
}